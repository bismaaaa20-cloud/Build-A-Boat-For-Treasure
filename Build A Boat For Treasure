-- =================================================================
-- LOGIKA UTAMA SKRIP AFK FARM MING - VERSI DETAIL PANJANG
-- =================================================================

local Stages = game:GetService("Workspace").BoatStages.NormalStages
local client = game:GetService("Players").LocalPlayer
local Penguin = workspace.ChangeCharacter
local HttpService = game:GetService("HttpService")

-- Konfigurasi Global
local cfg = getgenv().Config or {
    ["Setting_Farm"] = { ["Auto_Farm"] = true, ["FPS_Boost"] = true, ["Gold_Per_Stage"] = 16 },
    ["Anti_AFK"] = { ["Enable"] = true },
    ["Webhook"] = { ["UrlWebhook"] = "", ["Enable"] = true }
}

-- 1. FITUR FPS BOOST
if cfg["Setting_Farm"]["FPS_Boost"] then
    task.spawn(function()
        local descendants = workspace:GetDescendants()
        for i = 1, #descendants do
            local v = descendants[i]
            if v:IsA("Part") or v:IsA("CornerWedgePart") or v:IsA("TrussPart") or v:IsA("WedgePart") then
                v.Material = Enum.Material.SmoothPlastic
            elseif v:IsA("Decal") or v:IsA("Texture") then
                v:Destroy()
            end
        end
        game:GetService("Lighting").GlobalShadows = false
    end)
end

-- 2. FITUR ANTI-AFK
if cfg["Anti_AFK"]["Enable"] then
    local VirtualUser = game:GetService("VirtualUser")
    client.Idled:Connect(function()
        VirtualUser:CaptureController()
        VirtualUser:ClickButton2(Vector2.new(0,0))
    end)
end

-- 3. PANEL GUI
local screenGui = Instance.new("ScreenGui", game:GetService("CoreGui"))
local MainFrame = Instance.new("Frame", screenGui)
MainFrame.Size = UDim2.new(0, 290, 0, 240); MainFrame.Position = UDim2.new(0.5, -145, 0.5, -120)
MainFrame.BackgroundColor3 = Color3.fromRGB(30, 30, 30); MainFrame.Active = true; MainFrame.Draggable = true

-- 4. LOGIKA WEBHOOK
local function SendWebhookLog(gold)
    if cfg["Webhook"]["Enable"] and cfg["Webhook"]["UrlWebhook"] ~= "" then
        local data = { ["content"] = "Update: " .. client.Name .. " | Gold: " .. gold }
        request({Url = cfg["Webhook"]["UrlWebhook"], Method = "POST", Headers = {["content-type"] = "application/json"}, Body = HttpService:JSONEncode(data)})
    end
end

-- 5 & 6. ENGINE FARM (LOGIKA TELEPORT DIPISAH PER STAGE)
task.spawn(function()
    while cfg["Setting_Farm"]["Auto_Farm"] do
        local platform = Instance.new("Part", workspace)
        platform.Size = Vector3.new(35, 3, 35); platform.Anchored = true; platform.Transparency = 0.5

        -- Teleport Stage 1
        local s1 = Stages:FindFirstChild("CaveStage1")
        if s1 and s1:FindFirstChild("DarknessPart") then
            client.Character.HumanoidRootPart.CFrame = s1.DarknessPart.CFrame
            platform.CFrame = s1.DarknessPart.CFrame * CFrame.new(0, -4.5, 0)
            task.wait(5)
        end

        -- Teleport Stage 2
        local s2 = Stages:FindFirstChild("CaveStage2")
        if s2 and s2:FindFirstChild("DarknessPart") then
            client.Character.HumanoidRootPart.CFrame = s2.DarknessPart.CFrame
            platform.CFrame = s2.DarknessPart.CFrame * CFrame.new(0, -4.5, 0)
            task.wait(5)
        end

        -- Teleport Stage 3
        local s3 = Stages:FindFirstChild("CaveStage3")
        if s3 and s3:FindFirstChild("DarknessPart") then
            client.Character.HumanoidRootPart.CFrame = s3.DarknessPart.CFrame
            platform.CFrame = s3.DarknessPart.CFrame * CFrame.new(0, -4.5, 0)
            task.wait(3)
        end

        -- Teleport Stage 4
        local s4 = Stages:FindFirstChild("CaveStage4")
        if s4 and s4:FindFirstChild("DarknessPart") then
            client.Character.HumanoidRootPart.CFrame = s4.DarknessPart.CFrame
            platform.CFrame = s4.DarknessPart.CFrame * CFrame.new(0, -4.5, 0)
            task.wait(3)
        end

        -- Teleport Stage 5
        local s5 = Stages:FindFirstChild("CaveStage5")
        if s5 and s5:FindFirstChild("DarknessPart") then
            client.Character.HumanoidRootPart.CFrame = s5.DarknessPart.CFrame
            platform.CFrame = s5.DarknessPart.CFrame * CFrame.new(0, -4.5, 0)
            task.wait(3)
        end

        -- Teleport Stage 6
        local s6 = Stages:FindFirstChild("CaveStage6")
        if s6 and s6:FindFirstChild("DarknessPart") then
            client.Character.HumanoidRootPart.CFrame = s6.DarknessPart.CFrame
            platform.CFrame = s6.DarknessPart.CFrame * CFrame.new(0, -4.5, 0)
            task.wait(3)
        end

        -- Teleport Stage 7 (Cas)
        local s7 = Stages:FindFirstChild("CaveStage7")
        if s7 and s7:FindFirstChild("DarknessPart") then
            client.Character.HumanoidRootPart.CFrame = s7.DarknessPart.CFrame
            platform.CFrame = s7.DarknessPart.CFrame * CFrame.new(0, -4.5, 0)
            task.wait(1)
        end

        -- Teleport Stage 8
        local s8 = Stages:FindFirstChild("CaveStage8")
        if s8 and s8:FindFirstChild("DarknessPart") then
            client.Character.HumanoidRootPart.CFrame = s8.DarknessPart.CFrame
            platform.CFrame = s8.DarknessPart.CFrame * CFrame.new(0, -4.5, 0)
            task.wait(3.5)
        end

        -- Teleport Stage 9
        local s9 = Stages:FindFirstChild("CaveStage9")
        if s9 and s9:FindFirstChild("DarknessPart") then
            client.Character.HumanoidRootPart.CFrame = s9.DarknessPart.CFrame
            platform.CFrame = s9.DarknessPart.CFrame * CFrame.new(0, -4.5, 0)
            task.wait(3.5)
        end

        platform:Destroy()
        Penguin:FireServer("PenguinCharacter")
        task.wait(2)
    end
end)
